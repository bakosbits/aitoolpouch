import React from 'react';
import { AuthProvider } from '../context/AuthContext'; // Ensure this path is correct
import '../styles/globals.css'; // This imports your Tailwind CSS styles

function MyApp({ Component, pageProps }) {
  return (
    <AuthProvider> {/* All your pages and components are children of AuthProvider */}
      <Component {...pageProps} />
    </AuthProvider>
  );
}

export default MyApp;
